function largestOfFour(arr) {
  // builds a new array of the largest numbers from each sub-array 
  return arr.map(x => x = Math.max(...x));
}

largestOfFour([[4, 5, 1, 3], [13, 27, 18, 26], [32, 35, 37, 39], [1000, 1001, 857, 1]]); // returns [5, 27, 39, 1001]
